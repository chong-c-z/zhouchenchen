import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  base: '/zhouchenchen/', // 关键：必须与你的仓库名一致
  // ... 其他配置
})
